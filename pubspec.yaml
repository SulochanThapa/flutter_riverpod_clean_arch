name: flutter_riverpod_clean_arch
description: A clean architecture implementation with Riverpod

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  dio: ^5.3.2
  shared_preferences: ^2.2.0
  flutter_screenutil: ^5.9.0
  go_router: ^10.1.0
  either_dart: ^1.0.0
  internet_connection_checker: ^1.0.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  build_runner: ^2.4.6